name: Cross-Platform Release

permissions:
  contents: write

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

jobs:
  cross-platform-release:
    uses: pipery-dev/pipery-tooling/.github/workflows/pipery-cross-platform-release.yml@main
    with:
      repo_name: pipery-terraform-ci
    secrets: inherit
